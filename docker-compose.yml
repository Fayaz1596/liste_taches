version: '3.8'
services:
  express:
    build: back
    ports: 
      - "3000:3000"
    links:
      - bdd
  bdd:
    image: mongo
    ports:
      - "27017:27017"
  angular:
    build: front
    ports:
      - "4200:80"
    links: express:expressjs
